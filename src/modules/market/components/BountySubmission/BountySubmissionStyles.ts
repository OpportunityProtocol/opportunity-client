import { makeStyles } from "@mui/styles";
import { alpha } from "@mui/system";

const useStyles = makeStyles(theme => ({
    blockie: {
        borderRadius: 20
    },
    link: {
        cursor: 'pointer'
    }
}))

export { useStyles }