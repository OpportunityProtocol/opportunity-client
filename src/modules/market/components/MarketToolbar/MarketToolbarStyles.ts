import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => {
  return {
    container: {
      width: 400,
    },
    heading: {
      fontWeight: '500',
      color: 'rgba(33, 33, 33, .85)',
    },
  }
})

export { useStyles }
