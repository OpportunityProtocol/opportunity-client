import { makeStyles } from '@mui/styles'

const useStyles = makeStyles(theme => ({
    root: {
        width: '100%',
        height: '100vh',
        backgroundColor: '#fbfbfd',
    },
}))

export { useStyles }
