import React from 'react';
import { makeStyles } from '@mui/styles';

const useStyles = makeStyles(() => ({
  root: {
    maxWidth: 304,

    width: '100%'
  },
  content: {

  },
}));

export { useStyles }